<Page x:Class="MaxiMed.Wpf.Views.Theme.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignWidth="900" d:DesignHeight="650"
      Title="SettingsPage"
      Background="{DynamicResource BgBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24" Width="720">

            <TextBlock Text="Настройки"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Margin="0 0 0 16"/>

            <!-- LOOK & FEEL -->
            <Border Style="{StaticResource Card}" Margin="0 0 0 14">
                <StackPanel>
                    <TextBlock Text="Внешний вид" FontSize="16" FontWeight="SemiBold" Margin="0 0 0 12"/>

                    <DockPanel Margin="0 0 0 10">
                        <StackPanel>
                            <TextBlock Text="Тёмная тема"/>
                            <TextBlock Text="Как в Discord" Foreground="{DynamicResource TextMuted}" FontSize="12"/>
                        </StackPanel>
                        <CheckBox DockPanel.Dock="Right" IsChecked="{Binding IsDarkTheme}" VerticalAlignment="Center"/>
                    </DockPanel>

                    <TextBlock Text="Акцент" Foreground="{DynamicResource TextSecondary}" Margin="0 0 0 6"/>
                    <ComboBox ItemsSource="{Binding Accents}" SelectedItem="{Binding SelectedAccent}" />

                    <Grid Margin="0 12 0 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Плотность" Foreground="{DynamicResource TextSecondary}" Margin="0 0 0 6"/>
                            <ComboBox ItemsSource="{Binding Densities}" SelectedItem="{Binding SelectedDensity}" />
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Скругление" Foreground="{DynamicResource TextSecondary}" Margin="0 0 0 6"/>
                            <ComboBox ItemsSource="{Binding CornerPresets}" SelectedItem="{Binding SelectedCornerPreset}" />
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Размер шрифта" Foreground="{DynamicResource TextSecondary}" Margin="0 12 0 6"/>
                    <Slider Minimum="11" Maximum="16" Value="{Binding FontSize}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding FontSize, StringFormat=Текущий: {0:0}}" Foreground="{DynamicResource TextMuted}" FontSize="12" Margin="0 4 0 0"/>
                </StackPanel>
            </Border>

            <!-- ANIMATIONS -->
            <Border Style="{StaticResource Card}" Margin="0 0 0 14">
                <StackPanel>
                    <TextBlock Text="Анимации и эффекты" FontSize="16" FontWeight="SemiBold" Margin="0 0 0 12"/>

                    <DockPanel Margin="0 0 0 10">
                        <StackPanel>
                            <TextBlock Text="Анимации"/>
                            <TextBlock Text="Плавные переходы страниц" Foreground="{DynamicResource TextMuted}" FontSize="12"/>
                        </StackPanel>
                        <CheckBox DockPanel.Dock="Right" IsChecked="{Binding AnimationsEnabled}" VerticalAlignment="Center"/>
                    </DockPanel>

                    <TextBlock Text="Скорость" Foreground="{DynamicResource TextSecondary}" Margin="0 0 0 6"/>
                    <ComboBox ItemsSource="{Binding AnimationSpeeds}" SelectedItem="{Binding SelectedAnimationSpeed}" />
                </StackPanel>
            </Border>

            <!-- BEHAVIOR -->
            <Border Style="{StaticResource Card}" Margin="0 0 0 14">
                <StackPanel>
                    <TextBlock Text="Поведение" FontSize="16" FontWeight="SemiBold" Margin="0 0 0 12"/>

                    <CheckBox Content="Спрашивать подтверждение при удалении"
                              IsChecked="{Binding ConfirmDestructive}" />

                    <CheckBox Content="Запоминать последнюю страницу"
                              IsChecked="{Binding RememberLastPage}"
                              Margin="0 8 0 0"/>

                    <TextBlock Text="Стартовая страница" Foreground="{DynamicResource TextSecondary}" Margin="0 12 0 6"/>
                    <ComboBox ItemsSource="{Binding StartPages}" SelectedItem="{Binding SelectedStartPage}" />
                </StackPanel>
            </Border>

            <!-- SYSTEM -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="О системе" FontSize="16" FontWeight="SemiBold" Margin="0 0 0 12"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="Версия" Foreground="{DynamicResource TextSecondary}"/>
                            <TextBlock Text="{Binding AppVersion}" Margin="0 0 0 10"/>

                            <TextBlock Text="Соединение с базой" Foreground="{DynamicResource TextSecondary}"/>
                            <TextBlock Text="{Binding DbStatus}" />
                            <TextBlock Text="{Binding DbDetails}" Foreground="{DynamicResource TextMuted}" FontSize="12" TextWrapping="Wrap"/>
                        </StackPanel>

                        <Button Grid.Column="1"
                                Content="Проверить"
                                Style="{StaticResource SecondaryButton}"
                                Command="{Binding CheckConnectionCommand}"
                                VerticalAlignment="Top"/>
                    </Grid>

                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
